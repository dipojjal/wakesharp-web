---
/** /<locale>/account/delete — rendered for every enabled non-default locale. */
import AccountDeletePage from '../../../templates/AccountDeletePage.astro';
import { localeFromParams, localeStaticPaths } from '../../../i18n/routes';
export const getStaticPaths = localeStaticPaths;
---

<AccountDeletePage locale={localeFromParams(Astro.params)} />
