---
/** /<locale>/contact-sent — rendered for every enabled non-default locale. */
import ContactSentPage from '../../templates/ContactSentPage.astro';
import { localeFromParams, localeStaticPaths } from '../../i18n/routes';
export const getStaticPaths = localeStaticPaths;
---

<ContactSentPage locale={localeFromParams(Astro.params)} />
